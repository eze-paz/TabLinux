# Guest OS images (not bundled)

Prebuilt kernel/initramfs/disk images were removed to keep this repo small and
avoid redistributing GPL/third-party binaries. Build an Alpine riscv64 kernel +
initramfs and an ext4 rootfs and place them here. See ../docs/networking.md and
the build scripts (build-*.sh, kernels/mkrootfs.py).
